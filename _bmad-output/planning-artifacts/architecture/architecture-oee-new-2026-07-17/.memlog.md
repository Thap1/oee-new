---
scope: Web app OEE - kiến trúc tổng thể (Postgres/.NET/Angular+PrimeNG, multi-site on-premise)
purpose: build-substrate
altitude: feature
updated: 2026-07-17T00:00
---

- (event) architecture spine workspace tạo cho oee-new, input: prd.md + addendum.md (prd-oee-new-2026-07-17)
- (decision by user) chọn Coaching path
- (decision by user) chỉ cần bản spine thuần kỹ thuật, không cần tài liệu trình bày thêm
- (decision by user) [ADOPTED] paradigm: Layered Modular Monolith, replicated per site (mỗi site 1 instance độc lập, tự trị khi mất kết nối liên site) + module Sync riêng đẩy dữ liệu tổng hợp (không phải raw event) lên instance trung tâm. Trong mỗi instance: Domain (thuần, test độc lập, chứa logic tính OEE) / Application (use case) / Infrastructure (EF Core+Postgres, ingestion adapter, SignalR) / API (mỏng). Không CQRS/MediatR/Repository-pattern-vì-thích ở MVP.
- (decision by user) master data (máy, line, user, reason code) do TỪNG SITE tự quản lý (nguồn sự thật cục bộ); trung tâm chỉ tổng hợp để xem, không phải nơi cấu hình
- (constraint) hệ quả: mã lý do dừng máy có thể khác nhau giữa các site -> cần convention: mỗi site tự định nghĩa code riêng nhưng PHẢI map vào đúng 1 trong 3 nhóm tổn thất chuẩn toàn cục (Availability/Performance/Quality loss) để báo cáo trung tâm so sánh được xuyên site
- (decision by user) instance trung tâm dùng CÙNG một codebase với site, chạy ở chế độ trung tâm (tắt ingestion/dashboard xưởng, bật nhận đồng bộ + báo cáo xuyên site) - không tách service riêng
- (version) verified web 2026-07-17: .NET 10 LTS (hỗ trợ tới 11/2028, .NET 8 LTS sắp hết hạn 11/2026) - nguồn dotnet.microsoft.com/platform/support/policy
- (version) verified web 2026-07-17: Angular 22 (stable mới nhất, 6/2026) - nguồn angular.dev/reference/releases
- (version) verified web 2026-07-17: PrimeNG ~22.x (khớp version Angular 22) - nguồn npmjs.com/package/primeng
- (assumption) PrimeNG vừa chuyển tổ chức sang "PrimeUI" (repo primefaces/primeng đã archive) - cần double-check tên package/hướng dẫn cài đặt mới nhất lúc scaffold code thực tế, chưa chặn spine
- (version) verified web 2026-07-17: PostgreSQL 18 (bản major ổn định production mới nhất, PG19 còn beta) - nguồn postgresql.org/docs/release
- (decision by user) stack đồng ý: .NET 10, Angular 22, PrimeNG ~22.x, PostgreSQL 18
- (decision) mọi entity đồng bộ lên trung tâm dùng GUID/UUID làm khoá chính (sinh tại site, tránh đụng độ ID xuyên site khi đồng bộ)
- (decision by user) Admin là MỘT tài khoản đăng nhập trực tiếp được vào bất kỳ site nào (không phải account riêng ở trung tâm)
- (decision) hệ quả: Identity Provider trung tâm phát hành JWT có claim vai trò (Admin = claim toàn cục); mỗi site cache public key để tự xác thực chữ ký token offline (không cần gọi về trung tâm mỗi request); đăng nhập lần đầu/cấp token mới vẫn cần site liên lạc được trung tâm; user Operator/Manager/Viewer vẫn là dữ liệu cục bộ site
- (decision) [ADOPTED] real-time dashboard dùng SignalR hub trong từng site instance (đồng thuận từ phiên party-mode, Winston+Sally), FE Angular subscribe qua SignalR
- (decision) [ADOPTED] Ingestion Adapter Pattern: interface IProductionDataSource + 1 endpoint ingestion chuẩn hoá trong Infrastructure layer của site instance (từ addendum PRD)
- (event) đã thu thập đủ các invariant chính (paradigm, site/central split, master data locality, ID scheme, identity/auth, real-time, ingestion, stack) - chuyển sang soạn spine
- (event) đã distill ARCHITECTURE-SPINE.md: 8 AD (paradigm, site tự trị, ingestion adapter, master data locality, reason code taxonomy, GUID id, identity JWT offline-first, SignalR real-time), consistency conventions, stack, ERD, source tree, capability map, deferred
- (event) chạy Reviewer Gate: 3 subagent song song (rubric walker, version-verification, adversarial) - reviews lưu tại reviews/review-rubric.md, review-versions.md, review-adversarial.md
- (decision) fix critical: AD-4 vs AD-7 mâu thuẫn (User do site sở hữu nhưng central issue token) - tách rõ: credential đăng nhập do Identity Provider trung tâm tạo cho MỌI user (kể cả Operator/Manager/Viewer cục bộ), role-scoping (site/line nào) vẫn là dữ liệu site; đánh đổi: user mới cần site online tới trung tâm 1 lần để tạo credential, nhưng xác thực hàng ngày vẫn offline-first
- (decision) fix critical: AD-4 thêm rule cấm central proxy/forward ghi ngược xuống site kể cả dùng JWT Admin hợp lệ toàn cục - muốn ghi phải gọi thẳng site đó
- (decision) fix high: AD-2 làm rõ "tổng hợp" = bản ghi nghiệp vụ đã hoàn thiện (downtime event đã đóng, production count theo giờ, quality reject) không phải raw telemetry - khớp với entity liệt kê ở AD-6
- (decision) fix high: AD-3 định nghĩa rõ counter=cumulative (không phải delta), status=enum cố định Running|Stopped|Idle|Fault
- (decision) fix high: AD-6 đổi sang dùng uuidv7() gốc của Postgres 18 thay vì Guid.CreateVersion7() của .NET (có bug non-monotonic đã ghi nhận trên dotnet/runtime)
- (decision) fix medium: AD-7 thêm rule cache tối thiểu 2 JWKS signing key (hiện tại + trước) để token không bị invalidate ngay khi rotate lúc site offline
- (decision) fix medium: AD-5 khoá LossCategory là NOT NULL enum ở schema DB, không phải validation tầng ứng dụng tuỳ chọn
- (decision) fix medium: chọn @ngx-translate/core cho i18n (không dùng Angular built-in vì không hỗ trợ runtime switch theo FR-007)
- (decision) fix: thêm mục Backup/DR, Rollout, Observability, Dev/Staging vào Deployment & Environments (đa số để Deferred có lý do rõ, tránh "cả một chiều bị im lặng")
- (decision) fix: bổ sung FR-006/FR-017 vào Binds của AD-7 và Capability Map
- (open question) PrimeNG 22.0.0 vừa đổi license (Community/Commercial theo doanh thu/dev/nhân sự) + chưa xác nhận độc lập hỗ trợ Angular 22 chính thức (bản ổn định gần nhất xác nhận là Angular 21) - CẦN USER QUYẾT ĐỊNH, không tự fix
- (decision by user) lùi về Angular 21 + PrimeNG 21 (bản xác nhận tương thích chính thức, trước khi PrimeNG đổi license ở bản 22) - verified web: "PrimeNG v21 has landed - Angular v21 compatible"
- (event) Reviewer Gate hoàn tất, mọi finding đã fix hoặc resolve; cập nhật stack table Angular 21/PrimeNG 21
- (event) spine finalized
