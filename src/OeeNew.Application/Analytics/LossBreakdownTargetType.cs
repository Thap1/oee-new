namespace OeeNew.Application.Analytics;

/// <summary>Equipment = a single Machine; Area = a Production Area (Line), aggregating every Machine on it (FR-020).</summary>
public enum LossBreakdownTargetType
{
    Equipment,
    Area,
}
